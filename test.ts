import { createClient, OAuthStrategy } from '@wix/sdk';
import { checkout, orders } from '@wix/ecom';
console.log('RecordManuallyCollectedPayment:', orders.recordManuallyCollectedPayment.toString());
